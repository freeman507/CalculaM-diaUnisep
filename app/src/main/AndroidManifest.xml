<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.freeman.android.calculamediaunisep">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Calcula Média Unisep"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity"
            android:screenOrientation="portrait">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".Result" android:parentActivityName=".MainActivity"/>

        <activity android:name=".Result2" android:parentActivityName=".MainActivity"/>

        <activity android:name=".Result3" android:parentActivityName=".MainActivity"/>

    </application>

</manifest>